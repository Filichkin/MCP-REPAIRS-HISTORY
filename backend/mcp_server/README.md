# MCP Сервер истории ремонтов и обслуживания автомобилей

MCP (Model Context Protocol) сервер для получения информации об истории ремонтов, гарантийных обращений и технического обслуживания автомобилей по VIN номеру.

## Технологии и ресурсы

MCP сервер интегрируется со следующими внешними системами и технологиями:

### API источники данных

- **API истории ремонтов** - REST API для получения данных о гарантийных обращениях, техническом обслуживании и ремонтах автомобилей из внутренних систем дилерской сети
- **OAuth2 Client Credentials Flow** - аутентификация для доступа к защищенным API

### Managed RAG от Cloud.ru Evolution

- **Managed RAG** - сервис семантического поиска и извлечения информации из базы знаний с использованием технологий векторного поиска и LLM
- **Платформа Cloud.ru Evolution** - облачная платформа для развертывания AI/ML сервисов
- **База знаний гарантийной политики** - структурированная коллекция документов о законодательстве, правилах и процедурах гарантийного обслуживания

### MCP технологии

- **FastMCP** - фреймворк для создания MCP серверов
- **Server-Sent Events (SSE)** - протокол транспорта для взаимодействия MCP клиента и сервера
- **ToolResult** - стандартный формат ответов MCP инструментов с поддержкой структурированного контента

### Валидация и схемы

- **Pydantic v2** - валидация данных и генерация JSON Schema для `output_schema`
- **JSON Schema** - описание структуры выходных данных для каждого инструмента

## Описание

Сервер предоставляет инструменты для работы с API истории ремонтов и обслуживания автомобилей. Все данные возвращаются в формате **MCP ToolResult** с:
- **Текстовым описанием** (content) для LLM и пользователя
- **Структурированными JSON-данными** (structured_content) для программной обработки
- **Метаданными выполнения** (meta) с временем выполнения, источником данных и другой информацией

## Формат ответа

Все инструменты возвращают `ToolResult` со следующей структурой:

```json
{
  "content": [
    {
      "type": "text",
      "text": "Человекочитаемое описание результата..."
    }
  ],
  "structured_content": {
    "vin": "XWEG3417BN0009095",
    "total_records": 5,
    "data": {...}
  },
  "_meta": {
    "execution_time_ms": 145,
    "data_source": "warranty_api",
    "record_count": 5,
    "timestamp": "2025-12-07T12:00:00Z"
  }
}
```

При ошибках дополнительно устанавливается флаг `isError: true` с описанием проблемы.

---

## Доступные инструменты

### 1. `warranty_days(vin: str) -> ToolResult`

Получить статистику дней в ремонте по годам владения автомобиля.

**Параметры:**
- `vin` (str): VIN номер автомобиля

**Возвращает:**
- **content** - текстовое описание статистики ремонтов по годам
- **structured_content** - JSON с полными данными:
  ```json
  {
    "vin": "XWEG3417BN0009095",
    "total_years": 3,
    "repair_years": [
      {
        "year_number": 1,
        "is_current_year": false,
        "days_in_repair": 15
      },
      {
        "year_number": 2,
        "is_current_year": true,
        "days_in_repair": 8
      }
    ],
    "current_year_days": 8,
    "total_days_in_repair": 23
  }
  ```
- **meta** - метаданные с `execution_time_ms`, `data_source`, `record_count`, etc.

**Пример текстового вывода:**
```
Статистика ремонтов для VIN XWEG3417BN0009095:
Всего лет владения: 2
Общее количество дней в ремонте: 23

Год владения 1: 15 дней в ремонте
Год владения 2 (текущий): 8 дней в ремонте

В текущем году: 8 дней
```

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "vin": {"type": "string"},
    "total_years": {"type": "integer"},
    "repair_years": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "year_number": {"type": "integer"},
          "is_current_year": {"type": "boolean"},
          "days_in_repair": {"type": "integer"}
        }
      }
    },
    "current_year_days": {"type": ["integer", "null"]},
    "total_days_in_repair": {"type": "integer"}
  }
}
```

---

### 2. `warranty_history(vin: str) -> ToolResult`

Получить историю гарантийных обращений автомобиля.

**Параметры:**
- `vin` (str): VIN номер автомобиля

**Возвращает:**
- **content** - подробное описание всех гарантийных обращений
- **structured_content** - JSON с данными:
  ```json
  {
    "vin": "XWEG3417BN0009095",
    "records": [
      {
        "serial": "12345",
        "date": "2025-10-01",
        "odometer": 42284,
        "dealer": {
          "name": "ООО \"Молоток Авто\"",
          "code": "57002",
          "city": "Москва"
        },
        "fault_part": {
          "part_number": "76004Q5001",
          "description": "PANEL ASSY-FRONT DOOR,LH"
        },
        "replaced_parts": [
          {
            "part_number": "76004Q5001",
            "description": "PANEL ASSY-FRONT DOOR,LH"
          }
        ],
        "operations": [
          {
            "code": "76000R0B",
            "description": "FRONT DOOR PANEL ASSY, BOTH SIDES, R&R"
          }
        ]
      }
    ],
    "total_records": 1,
    "total_parts_replaced": 1,
    "total_operations": 1
  }
  ```
- **meta** - метаданные выполнения

**Output Schema:**
Структура включает массив `records` с полными данными о гарантийных обращениях, замененных деталях и выполненных работах.

---

### 3. `maintenance_history(vin: str) -> ToolResult`

Получить историю технического обслуживания автомобиля.

**Параметры:**
- `vin` (str): VIN номер автомобиля

**Возвращает:**
- **content** - описание всех записей технического обслуживания
- **structured_content** - JSON с данными:
  ```json
  {
    "vin": "XWEG3417BN0009095",
    "records": [
      {
        "vin": "XWEG3417BN0009095",
        "maintenance_type": "ТО-1",
        "date": "2025-08-15",
        "odometer": 15000,
        "dealer": {
          "name": "ООО \"Молоток Авто\"",
          "code": "12345",
          "city": "Москва"
        }
      }
    ],
    "total_records": 1,
    "maintenance_types": ["ТО-1", "ТО-2"]
  }
  ```
- **meta** - метаданные выполнения

**Output Schema:**
Структура включает массив `records` с данными о ТО и уникальные типы обслуживания.

---

### 4. `vehicle_repairs_history(vin: str) -> ToolResult`

Получить историю ремонтов из дилерской сети (DNM records).

**Параметры:**
- `vin` (str): VIN номер автомобиля

**Возвращает:**
- **content** - описание всех ремонтов из дилерской сети
- **structured_content** - JSON с данными:
  ```json
  {
    "vin": "XWEG3417BN0009095",
    "records": [
      {
        "dealer_name": "ООО \"Молоток Авто\"",
        "date": "2025-10-01",
        "odometer": 42284,
        "repair_type": "текущий ремонт",
        "visit_reason": "замена двери по гарантии(КУЗОВНОЙ)",
        "recommendations": "Все работы выполнены в полном объеме."
      }
    ],
    "total_records": 1,
    "repair_types": ["текущий ремонт", "гарантийный ремонт"]
  }
  ```
- **meta** - метаданные выполнения

**Output Schema:**
Структура включает массив `records` с данными о ремонтах DNM и уникальные типы ремонтов.

---

### 5. `compliance_rag(query: str) -> ToolResult`

> **⚠️ ВАЖНО:** Этот инструмент использует **Managed RAG от Платформы Cloud.ru Evolution**.
>
> Managed RAG - это сервис для семантического поиска и извлечения информации из базы знаний с использованием технологий векторного поиска и LLM. Требует настройки подключения к Cloud.ru Evolution и наличия активной базы знаний.

Поиск релевантных документов в базе знаний с использованием RAG (Retrieval-Augmented Generation).

**Параметры:**
- `query` (str): Запрос пользователя на естественном языке

**Возвращает:**
- **content** - отформатированная строка с релевантными документами
- **structured_content** - JSON с данными:
  ```json
  {
    "query": "Что делать в случае повторного ремонта?",
    "documents": [
      {
        "content": "В случае повторного ремонта по той же причине...",
        "metadata": {
          "source": "warranty_policy.pdf",
          "page": 15
        },
        "relevance_score": 0.95
      }
    ],
    "total_documents": 3,
    "knowledge_base_version": "latest"
  }
  ```
- **meta** - метаданные с временем выполнения, параметрами поиска и информацией о базе знаний

**Возможности:**
- Семантический поиск по базе знаний (retrieval_type: SEMANTIC)
- Автоматическая аутентификация через OAuth2 (Client Credentials Flow)
- Кэширование токенов с автоматическим обновлением при истечении
- Настраиваемое количество результатов (параметр `RETRIEVE_LIMIT`)
- Graceful обработка ошибок с возвратом ToolResult с `isError: true`

**Output Schema:**
```json
{
  "type": "object",
  "properties": {
    "query": {"type": "string"},
    "documents": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "content": {"type": "string"},
          "metadata": {"type": "object"},
          "relevance_score": {"type": ["number", "null"]}
        }
      }
    },
    "total_documents": {"type": "integer"},
    "knowledge_base_version": {"type": "string"}
  }
}
```

**Обработка ошибок:**

Все ошибки обрабатываются gracefully - вместо исключений возвращается `ToolResult` с `isError: true`:

- **Authentication error** - Ошибка при получении токена доступа
- **401 Unauthorized** - Автоматическое обновление токена и повторный запрос (до 2 попыток)
- **HTTP errors** - Детальное описание HTTP ошибки с кодом статуса
- **Timeout** - Превышено время ожидания (20 секунд), информативное сообщение пользователю
- **Network errors** - Сетевые ошибки при подключении к Managed RAG API
- **Unexpected errors** - Неожиданные ошибки с рекомендацией обратиться к администратору

**Пример ответа при ошибке:**
```json
{
  "content": [{
    "type": "text",
    "text": "База знаний временно недоступна (таймаут запроса). Пожалуйста, попробуйте позже или обратитесь к администратору."
  }],
  "isError": true,
  "_meta": {
    "query": "ваш запрос",
    "error_type": "timeout",
    "execution_time_ms": 20145,
    "timestamp": "2025-12-07T12:02:40Z"
  }
}
```

**Настройка Cloud.ru Evolution Managed RAG:**

Для работы `compliance_rag` необходимо:
1. Создать проект в Cloud.ru Evolution
2. Создать базу знаний (Knowledge Base)
3. Загрузить документы в базу знаний
4. Получить OAuth2 credentials (Client ID и Client Secret)
5. Настроить параметры в `.env` (см. раздел "Конфигурация")

---

## Запуск сервера

### Основной запуск

Для запуска MCP сервера выполните команду из директории `backend`:

```bash
python -m mcp_server.server
```

Сервер запустится на порту, указанном в конфигурации (по умолчанию `8004`), и будет доступен по адресу:
- **SSE endpoint:** `http://localhost:8004/sse`
- **Messages endpoint:** `http://localhost:8004/messages/`

При запуске сервер выведет информацию о:
- Адресе сервера
- Доступных эндпоинтах
- Списке доступных инструментов
- Настройках API
- Использовании структурированных JSON-ответов

### Тестирование

Для тестирования работы сервера и всех инструментов используйте тестовый скрипт:

```bash
python -m mcp_server.test_server
```

Тестовый скрипт:
1. Подключается к запущенному MCP серверу
2. Проверяет доступность всех инструментов
3. Выполняет тестовые запросы для каждого инструмента с тестовым VIN номером
4. Выводит результаты выполнения (текстовое содержимое и структурированные данные)

**Важно:** Перед запуском тестов убедитесь, что основной сервер запущен и доступен по адресу `http://localhost:8004/sse`.

---

## Конфигурация

Настройки сервера находятся в файле `backend/config.py` и могут быть переопределены через переменные окружения в файле `infra/.env`.

### MCP Server параметры:
- `mcp_server_url` - URL MCP сервера (по умолчанию: `http://localhost:8004`)
- `mcp_transport` - тип транспорта (по умолчанию: `sse`)
- `mcp_server_port` - порт сервера (по умолчанию: `8004`)
- `mcp_server_host` - хост сервера (по умолчанию: `127.0.0.1`)

### Vehicle Repairs API параметры:
- `api_key` - Bearer токен для аутентификации в API
- `api_url` - URL внешнего API (по умолчанию: `http://127.0.0.1:8000`)

### Cloud.ru Evolution Managed RAG параметры:

> **ℹ️ Для инструмента `compliance_rag`**

- `key_id` - Client ID для OAuth2 аутентификации в Cloud.ru Evolution
- `key_secret` - Client Secret для OAuth2 аутентификации
- `auth_url` - URL эндпоинта аутентификации (получение токена)
  - Пример: `https://auth.iam.sbercloud.ru/auth/system/openid/token`
- `retrieve_url_template` - URL эндпоинта Managed RAG для поиска в базе знаний
  - Пример: `https://your-instance.managed-rag.inference.cloud.ru/api/v2/retrieve`
- `knowledge_base_id` - Идентификатор базы знаний в Cloud.ru Evolution
- `knowledge_base_version_id` - Версия базы знаний (по умолчанию: `latest`)
- `retrieve_limit` - Максимальное количество результатов поиска (по умолчанию: `3`)
- `evolution_project_id` - Идентификатор проекта в Cloud.ru Evolution

### Пример файла `.env`:

Скопируйте `infra/.env.example` в `infra/.env` и заполните необходимые значения:

```bash
# MCP Server Configuration
MCP_SERVER_PORT=8004
MCP_SERVER_HOST='127.0.0.1'

# Vehicle Repairs API
API_KEY=your-api-key-here
API_URL=http://your-api-url

# Cloud.ru Evolution Managed RAG Configuration
KEY_ID=your-client-id
KEY_SECRET=your-client-secret
AUTH_URL=https://auth.iam.sbercloud.ru/auth/system/openid/token
RETRIEVE_URL_TEMPLATE=https://your-instance.managed-rag.inference.cloud.ru/api/v2/retrieve
KNOWLEDGE_BASE_ID=your-kb-id
KNOWLEDGE_BASE_VERSION_ID=latest
RETRIEVE_LIMIT=3
EVOLUTION_PROJECT_ID=your-project-id
```

---

## Аутентификация

### Vehicle Repairs API
Использует Bearer token аутентификацию. Токен должен быть указан в конфигурации через переменную `api_key`.

### Cloud.ru Evolution Managed RAG API

> **🔐 OAuth2 Client Credentials Flow**

Автоматическая аутентификация с кэшированием токенов:

1. При первом запросе к `compliance_rag` сервер автоматически получает access token через OAuth2
2. Токен кэшируется в памяти для повторного использования
3. При истечении токена (HTTP 401) автоматически выполняется обновление и повторный запрос
4. Все запросы защищены асинхронной блокировкой (`asyncio.Lock`) для thread-safety
5. При ошибках аутентификации возвращается ToolResult с `isError: true`

**Необходимые credentials:**
- `KEY_ID` - Client ID из Cloud.ru Evolution
- `KEY_SECRET` - Client Secret из Cloud.ru Evolution
- `AUTH_URL` - эндпоинт для получения токена (IAM сервис)

---

## Обработка ошибок

Сервер корректно обрабатывает следующие ситуации с возвратом `ToolResult`:

### Vehicle Repairs API:
- **404 Not Found** - VIN не найден (логируется как INFO, возвращается ToolResult с пустыми данными)
- **401 Unauthorized** - Ошибка аутентификации (возвращается ToolResult с `isError: true`)
- **Timeout** - Превышено время ожидания запроса (возвращается ToolResult с описанием ошибки)
- **Другие HTTP ошибки** - Возвращается ToolResult с деталями ошибки

### Cloud.ru Evolution Managed RAG API:
- **Authentication errors** - Ошибки при получении токена (возвращается ToolResult с `isError: true`)
- **401 Unauthorized** - Автоматическое обновление токена и повторная попытка (до 2 раз)
- **HTTP errors** - Детальное описание ошибки с HTTP статусом
- **Timeout** - Информативное сообщение пользователю (таймаут: 20 секунд)
- **Network errors** - Описание сетевой проблемы с рекомендациями
- **Unexpected errors** - Общая обработка с рекомендацией обратиться к администратору

### Логирование

Все инструменты логируют:
- ✅ **Вызов tool** - параметры запроса (VIN или query)
- ✅ **Успешные результаты** - количество найденных записей/документов, наличие структурированных данных
- ⚠️ **Пустые результаты** - отсутствие данных для запроса
- ⚠️ **Ошибки с is_error=True** - флаг ошибки в ответе инструмента
- ❌ **Исключения** - детальное описание с кодами и сообщениями

Логи доступны в консоли при запуске сервера с использованием библиотеки `loguru`.

---

## Интеграция с агентами

### MCPClient

Клиент `backend/agent/tools/mcp_client.py` автоматически обрабатывает новый формат ответов:

```python
from backend.agent.tools.mcp_client import get_mcp_client

async with await get_mcp_client() as client:
    response = await client.warranty_days('XWEG3417BN0009095')

    # Обратная совместимость - текстовое содержимое
    text_result = response['result']

    # Новые возможности - структурированные данные
    if 'structured_content' in response:
        structured = response['structured_content']
        total_days = structured['total_days_in_repair']
        current_year_days = structured.get('current_year_days')

    # Метаданные выполнения
    if 'meta' in response:
        execution_time = response['meta'].get('execution_time_ms')
        data_source = response['meta'].get('data_source')

    # Проверка ошибок
    if response.get('is_error'):
        print(f'Tool error: {text_result}')
```

**Возвращаемая структура:**
```python
{
    'result': str,                    # Текст (обратная совместимость)
    'structured_content': dict,       # JSON данные (опционально)
    'meta': dict,                     # Метаданные (опционально)
    'is_error': bool                  # Флаг ошибки (опционально)
}
```

---

## Зависимости

Основные зависимости:
- `fastmcp>=2.13.3` - фреймворк для создания MCP серверов
- `mcp>=1.3.2` - MCP protocol типы и классы
- `httpx>=0.27.0` - асинхронный HTTP клиент
- `loguru>=0.7.3` - логирование
- `pydantic>=2.5.0` - валидация данных и модели
- `pydantic-settings>=2.7.1` - управление настройками

---

## Структура проекта

```
backend/
├── mcp_server/
│   ├── __init__.py
│   ├── server.py          # Основной файл сервера с инструментами
│   ├── models.py          # Pydantic модели для structured_content
│   ├── formatters.py      # Функции форматирования текста
│   ├── test_server.py     # Тестовый скрипт
│   └── README.md          # Документация
├── agent/
│   └── tools/
│       └── mcp_client.py  # Клиент для взаимодействия с MCP сервером
├── config.py              # Конфигурация
└── pyproject.toml         # Зависимости проекта
```

---

## Технические детали реализации

### Pydantic модели (models.py)

Для структурированных данных используются Pydantic модели с валидацией:

- `WarrantyDaysStructured` - статистика дней в ремонте
- `WarrantyHistoryStructured` - история гарантийных обращений
- `MaintenanceHistoryStructured` - история ТО
- `VehicleRepairsHistoryStructured` - история ремонтов DNM
- `ComplianceRAGStructured` - результаты поиска в базе знаний

**Особенности:**
- Автоматическая валидация типов данных
- Конвертация типов (например, `int` → `str` для кодов дилеров)
- Генерация JSON Schema для `output_schema`

### Форматтеры (formatters.py)

Функции для создания человекочитаемого текста из структурированных данных:

- `format_warranty_days_text()` - форматирование статистики ремонтов
- `format_warranty_history_text()` - форматирование истории гарантийных обращений
- `format_maintenance_history_text()` - форматирование истории ТО
- `format_vehicle_repairs_history_text()` - форматирование истории ремонтов DNM
- `format_compliance_rag_text()` - форматирование результатов RAG

### ToolResult

Каждый инструмент возвращает `fastmcp.tools.tool.ToolResult`:

```python
from fastmcp.tools.tool import ToolResult
from mcp.types import TextContent

return ToolResult(
    content=[TextContent(type='text', text=formatted_text)],
    structured_content=structured_model.model_dump(),
    meta={
        'execution_time_ms': int((time.time() - start_time) * 1000),
        'data_source': 'warranty_api',
        'record_count': len(records),
        'timestamp': time.strftime('%Y-%m-%dT%H:%M:%SZ', time.gmtime())
    }
)
```

---

## Лицензия

Проект разработан для внутреннего использования.
